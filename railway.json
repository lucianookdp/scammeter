{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @scammeter/core build && pnpm --filter @scammeter/proxy build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @scammeter/proxy start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
